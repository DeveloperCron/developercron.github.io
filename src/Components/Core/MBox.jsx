import * as React from 'react';
import Box from '@mui/material/Box';

const BoxElement = () => {
    return (
        <Box
            sx={{
                width: 1 / 1,
                height: 100,
            }}
        />
    );
}

export default BoxElement